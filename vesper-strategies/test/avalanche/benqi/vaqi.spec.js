'use strict'

// const testRunner = require('../../utils/testRunner')

// Commenting out tests as QI minting is paused on Benqi.
describe('VAQI Pool', function () {
  // testRunner('VAQI', ['Benqi_QI'], [{ debtRatio: 9600 }])
})
